# ??????????? 2/2:??????(kind: JobQueue,?????? CRD ?)?
# ? job-queue-example-rs.yaml ??,???? / Stream / ???? / ????????
#
# ??:od ???? od-worker(??????,od.jobs / od-workers / od-worker);
#       ???????? 000011_job_queues.up.sql ??,??????????
#
# ????:metadata.annotations ? cloud.satellite.io/delete: "true" ? apply-config,
#   ? ?????????????????

apiVersion: cloud.satellite.io/v1
kind: JobQueue
metadata:
  name: od
  labels:
    app.kubernetes.io/part-of: satellite-cloud
spec:
  name: od
  stream: od.jobs
  consumerGroup: od-workers
  consumerPrefix: od-worker
  concurrency: 1
  mode: external
  # ????????? Redis,?????????;???????????
  redisAddr: ""
  maxLen: 100000
  enabled: true
