# Pilot：与现有 satellite-backend 同 namespace，共享 PVC/Secret
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitlab-runner

resources:
  - redis/deployment.yaml
  - rs-worker/serviceaccount.yaml
  - rs-worker/deployment.yaml

labels:
  - includeSelectors: true
    pairs:
      satellite.io/phase: phase1-pilot
