apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitlab-runner

resources:
  - od-worker/deployment.yaml

labels:
  - includeSelectors: true
    pairs:
      satellite.io/phase: phase2-pilot
